// src/index.ts
import useBroadcastChannel from './useBroadcastChannel';

// Named export for specific import
export { useBroadcastChannel };

// Default export for convenience
export default useBroadcastChannel;
